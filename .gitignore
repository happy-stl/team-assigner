# Go build output (root-level binary only; must not match cmd/team-assigner/)
/team-assigner
*.exe
*.out
/bin/
/dist/

# Go caches and coverage
*.test
*.coverprofile
coverage.html

# Survey responses are real people's data: never track them.
# Only hand-made synthetic examples under testdata/ are committed.
*.csv
!testdata/**/*.csv

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp
*~
